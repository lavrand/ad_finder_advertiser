import{fetchBranches}from"../requests/requests";import{createAction}from"../utils/actions";import{actions}from"../consts/actions";import{flowTypes}from"../consts/flow";import{renderClickableList}from"../components/clickable-list";import{Context}from"telegraf";export const getBranchesSellCtrl=async a=>{let{data:b}=await fetchBranches(a);const c=b.map(a=>({title:a.name,action:createAction(actions.services,[a._id,flowTypes.sell])}));return await renderClickableList(a,"Please, choose branch",c)};export const getBranchesBuyCtrl=async a=>{let b=await fetchBranches(a);return b=b.map(a=>({title:a.name,action:createAction(actions.services,[a._id,flowTypes.buy])})),await renderClickableList(a,"Choose branch",b)};